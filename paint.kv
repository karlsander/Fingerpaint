
	
<MainMenuWidget>:
	AnchorLayout:

		size: app.win.width, app.win.height
		anchor_x: 'center'
		anchor_y: 'bottom'
		Button:

			text: 'back'
			size_hint: 0.5, 0.05
			on_press: app.manager.current = 'paint screen'



<StrokePickerWidget>:

	GridLayout:
		size: root.width/2, root.height/2
		pos: root.center_x - self.width/2, root.center_y - self.height/2
		cols: 1
		rows: 3
		Label:
			text: 'Stroke size'
			font_size: 50
		Slider:


	AnchorLayout:

		size: app.win.width, app.win.height
		anchor_x: 'center'
		anchor_y: 'bottom'
		Button:

			text: 'back'
			size_hint: 0.5, 0.05
			on_press: app.manager.current = 'paint screen'



<ColorPickerWidget>:
	GridLayout:
		size: root.width/2, root.height/2
		pos: root.center_x - self.width/2, root.center_y - self.height/2
		cols: 10
		rows: 3
		
		Button:
			background_color: 0.1,0.2,0.3,1
			on_press: root.set_color(0.1,0.2,0.3,1)
		Button:
			background_color: 0.3,0.2,0.3,1
			on_press: root.set_color(0.3,0.2,0.3,1)
		Button:
			background_color: 0.1,1,0.3,1
			on_press: root.set_color(0.1,1,0.3,1)
		Button:
			background_color: 0.1,0.2,1,1
			on_press: root.set_color(0.1,0.2,1,1)


	AnchorLayout:

		size: app.win.width, app.win.height
		anchor_x: 'center'
		anchor_y: 'bottom'
		Button:

			text: 'back'
			size_hint: 0.5, 0.05
			on_press: app.manager.current = 'paint screen'

<PaintWidget>:


<MenuWidget>:
	size: app.win.width, app.win.height

	AnchorLayout:

		size: app.win.width, app.win.height
		anchor_x: 'right'
		anchor_y: 'center'
		Button:

			text: 'Color'
			size_hint: 0.05, 0.5
			on_press: app.manager.current = 'color screen'

	AnchorLayout:

		size: app.win.width, app.win.height
		anchor_x: 'center'
		anchor_y: 'top'
		Button:

			text: 'Stroke'
			size_hint: 0.5, 0.05
			on_press: app.manager.current = 'stroke screen'

	AnchorLayout:

		size: app.win.width, app.win.height
		anchor_x: 'left'
		anchor_y: 'center'
		Button:
			text: 'Menu'
			size_hint: 0.05, 0.5
			on_press: app.manager.current = 'menu screen'
	
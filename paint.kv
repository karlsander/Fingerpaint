<RootWidget>:

	

<PaintWidget>:


<MenuWidget>:
	size: self.width, self.height

	AnchorLayout:

		size: root.width, root.height
		anchor_x: 'right'
		anchor_y: 'center'
		Button:

			text: 'Color'
			size_hint: 0.05, 0.5

	AnchorLayout:

		size: root.width, root.height
		anchor_x: 'center'
		anchor_y: 'top'
		Button:

			text: 'Stroke'
			size_hint: 0.5, 0.05

	AnchorLayout:

		size: root.width, root.height
		anchor_x: 'left'
		anchor_y: 'center'
		Button:
			text: 'Menu'
			size_hint: 0.05, 0.5
	